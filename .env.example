# Database
DATABASE_URL="file:./dev.db"

# For production (Netlify), you might want to use a different database
# DATABASE_URL="postgresql://user:password@host:port/database"
# or use a service like PlanetScale, Supabase, or Neon
