# 📂 دليل الملفات للرفع على GitHub

## ✅ الملفات التي يجب رفعها

### 📁 الملفات الأساسية

```
✅ يجب رفعها
├── app/                          # جميع ملفات التطبيق
├── components/                   # جميع المكونات
├── lib/                          # المكتبات والدوال المساعدة
├── prisma/                       # مخطط قاعدة البيانات
│   ├── schema.prisma            ✅
│   └── seed.js                  ✅ (إن وجد)
├── public/                       # الملفات الثابتة
│   └── _headers                 ✅
├── .gitignore                   ✅
├── .env.example                 ✅ (نموذج فقط - بدون بيانات حساسة)
├── netlify.toml                 ✅
├── next.config.js               ✅
├── package.json                 ✅
├── package-lock.json            ✅
├── postcss.config.js            ✅
├── tailwind.config.ts           ✅
├── tsconfig.json                ✅
├── next-env.d.ts                ✅
├── README.md                    ✅
├── DEPLOYMENT.md                ✅
├── QUICK_START.md               ✅
├── NETLIFY_ENV.md               ✅
├── NETLIFY_SETUP_COMPLETE.md    ✅
├── CHECKLIST.md                 ✅
├── SETUP_SUMMARY.txt            ✅
├── prepare_deploy.bat           ✅
└── netlify-build.sh             ✅
```

---

## ❌ الملفات التي يجب عدم رفعها

هذه الملفات **محمية تلقائياً** بواسطة `.gitignore`:

```
❌ لا ترفعها (محمية بـ .gitignore)
├── .env                         ❌ (يحتوي على بيانات حساسة!)
├── .env.local                   ❌
├── .env*.local                  ❌
├── node_modules/                ❌ (كبير جداً - سيتم تثبيته تلقائياً)
├── .next/                       ❌ (ملفات البناء - تُنشأ تلقائياً)
├── .netlify/                    ❌ (ملفات Netlify المحلية)
├── .vercel/                     ❌
├── *.db                         ❌ (قاعدة البيانات المحلية)
├── *.db-journal                 ❌
├── /prisma/migrations/          ❌ (اختياري - حسب استراتيجيتك)
└── /lib/generated/prisma/       ❌
```

---

## 🔒 ملفات حساسة - احذر!

### ⚠️ لا ترفع أبداً:

1. **`.env`** - يحتوي على:
   - `DATABASE_URL` (معلومات الاتصال بقاعدة البيانات)
   - أي مفاتيح API
   - أي كلمات مرور

2. **`*.db`** - ملفات قاعدة البيانات المحلية

3. **`node_modules/`** - مجلد التبعيات (كبير جداً)

### ✅ بدلاً من ذلك:

- ارفع **`.env.example`** - نموذج بدون بيانات حقيقية
- استخدم **Environment Variables** في Netlify

---

## 📋 خطوات الرفع على GitHub

### 1. تحقق من .gitignore

تأكد من أن ملف `.gitignore` موجود ويحتوي على:

```gitignore
# local env files
.env
.env*.local

# dependencies
/node_modules

# next.js
/.next/
/out/

# netlify
.netlify

# prisma
*.db
*.db-journal
```

✅ **تم بالفعل** - الملف جاهز!

---

### 2. تهيئة Git (إذا لم يكن مهيئاً)

```bash
git init
```

---

### 3. إضافة جميع الملفات

```bash
git add .
```

هذا الأمر سيضيف **جميع الملفات** ما عدا المذكورة في `.gitignore`

---

### 4. التحقق من الملفات المضافة

```bash
git status
```

**تحقق من أن**:
- ✅ `.env` **غير** موجود في القائمة
- ✅ `node_modules/` **غير** موجود
- ✅ `.next/` **غير** موجود
- ✅ `package.json` موجود
- ✅ `netlify.toml` موجود

---

### 5. عمل Commit

```bash
git commit -m "Initial commit - Ready for Netlify deployment"
```

---

### 6. إنشاء مستودع على GitHub

1. اذهب إلى https://github.com
2. اضغط **"New repository"**
3. اختر اسماً للمستودع (مثل: `employee-management-system`)
4. **لا تضف** README أو .gitignore (موجودان بالفعل)
5. اضغط **"Create repository"**

---

### 7. ربط المستودع المحلي مع GitHub

```bash
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
git branch -M main
git push -u origin main
```

**استبدل**:
- `YOUR_USERNAME` باسم المستخدم الخاص بك
- `YOUR_REPO_NAME` باسم المستودع

---

## 🔍 التحقق من الملفات المرفوعة

بعد الرفع، تحقق من GitHub:

### ✅ يجب أن ترى:
- ✅ `package.json`
- ✅ `netlify.toml`
- ✅ `next.config.js`
- ✅ `.gitignore`
- ✅ `.env.example`
- ✅ مجلدات: `app/`, `components/`, `lib/`, `prisma/`, `public/`
- ✅ ملفات التوثيق: `README.md`, `DEPLOYMENT.md`, إلخ

### ❌ يجب ألا ترى:
- ❌ `.env`
- ❌ `node_modules/`
- ❌ `.next/`
- ❌ `*.db`

---

## 🆘 إذا رفعت ملف .env بالخطأ

### إزالة .env من Git:

```bash
# إزالة من Git (لكن يبقى محلياً)
git rm --cached .env

# Commit التغيير
git commit -m "Remove .env from repository"

# رفع التحديث
git push
```

### ⚠️ مهم جداً:
إذا رفعت `.env` بالخطأ:
1. احذفه من Git فوراً
2. **غيّر جميع كلمات المرور** و API keys الموجودة فيه
3. أنشئ `DATABASE_URL` جديد

---

## 📊 ملخص سريع

| الملف/المجلد | رفع؟ | السبب |
|--------------|------|-------|
| `app/` | ✅ نعم | كود التطبيق |
| `components/` | ✅ نعم | مكونات UI |
| `lib/` | ✅ نعم | دوال مساعدة |
| `prisma/schema.prisma` | ✅ نعم | مخطط قاعدة البيانات |
| `public/` | ✅ نعم | ملفات ثابتة |
| `package.json` | ✅ نعم | التبعيات |
| `netlify.toml` | ✅ نعم | تكوين Netlify |
| `.env.example` | ✅ نعم | نموذج فقط |
| `.env` | ❌ لا | بيانات حساسة! |
| `node_modules/` | ❌ لا | كبير - يُثبت تلقائياً |
| `.next/` | ❌ لا | يُبنى تلقائياً |
| `*.db` | ❌ لا | قاعدة بيانات محلية |

---

## ✅ قائمة تحقق نهائية

قبل الرفع:

- [ ] تأكد من وجود `.gitignore`
- [ ] تأكد من أن `.env` **غير** مضاف
- [ ] راجع `git status` قبل الـ commit
- [ ] تأكد من أن `package.json` موجود
- [ ] تأكد من أن `netlify.toml` موجود
- [ ] أضف رسالة commit واضحة

بعد الرفع:

- [ ] تحقق من الملفات على GitHub
- [ ] تأكد من عدم وجود `.env`
- [ ] تأكد من عدم وجود `node_modules/`
- [ ] جاهز للربط مع Netlify! 🚀

---

## 🎯 الخطوة التالية

بعد رفع الملفات على GitHub:
1. اذهب إلى [`QUICK_START.md`](./QUICK_START.md)
2. اتبع **الخطوة 3: ربط مع Netlify**

---

**الملفات جاهزة للرفع! ابدأ الآن! 🚀**
